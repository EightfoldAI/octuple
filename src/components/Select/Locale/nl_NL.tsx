import type { SelectLocale } from '../Select.types';

const locale: SelectLocale = {
  lang: {
    locale: 'nl_NL',
    currentlySelectedText: 'momenteel geselecteerde optie',
  },
};

export default locale;
